# Orchestrator variables for cloud mode
DEPLOY_ENV=cloud
RUN_ON_CLOUD=true
PORT=4000
# Preferred on Render: inject from Key Value connection string
REDIS_URL=
# Fallback if not using REDIS_URL
REDIS_HOST=your-render-redis-host
REDIS_PORT=6379
